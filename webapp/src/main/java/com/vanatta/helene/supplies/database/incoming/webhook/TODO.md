Move the classes in this package to 'domain' specific packages.
"incoming/webhooks" is generic, not good, does not describe
"what" is in here - just describes how (which is not good!).

EG: "needs.matching" package, etc...